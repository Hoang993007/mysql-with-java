have to notice the space like
 +" where


Special note: Find out more!!!!!

if use update, insert, delete
must use  stmt.executeUpdate(sql);

if use select
must use stmt.executeQuery();

những đoạn code lặp lại thì hãy nghĩ cách để viết chúng thành cùng 1 modul với nhiều lựa chọn

nếu dùng if else thì vế ngắn viết trước, vế dài viết sau sẽ dễ theo dõi hơn

*) When complie, have to wirte file name are complied in order that let the the package a file need to use have create before
